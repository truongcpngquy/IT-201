#include <stdio.h>

int main()
{
    int n = 10; // Ví dụ gán n để code chạy thử
    // Độ phức tạp thời gian: O(n)
    for (int i = 0; i < n; i++)
    {
        printf("Hello\n");
    }
    return 0;
}
